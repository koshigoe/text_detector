require 'spec_helper'

describe TextDetector do
  it 'has a version number' do
    expect(TextDetector::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
